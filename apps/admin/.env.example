# 仅服务端使用（Route Handlers / Server Components），不要以 NEXT_PUBLIC_ 前缀暴露给浏览器
BACKEND_BASE_URL=http://localhost:3002

# Admin 的 basePath（必须以 / 开头；不要以 / 结尾；例如 /admin）
# 注意：Next.js 的 basePath 是构建/启动时生效，修改后需要重启 dev server。
NEXT_PUBLIC_ADMIN_BASE_PATH=/admin
