# 本地开发示例（默认监听 3002，避免与其他服务端口冲突）
PORT=3002
NODE_ENV=development

# CORS allow origin for apps/admin
ADMIN_ORIGIN=http://localhost:3001

# JWT
JWT_SECRET=change-me
JWT_EXPIRES_IN=12h

# Auth sessions (refresh token)
AUTH_REFRESH_TOKEN_TTL_DAYS=30

# 数据库配置（通用于本地/自建/云 MySQL）
# 约定：统一使用 DB_*。
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=snapmatch_user
DB_PASSWORD=
DB_DATABASE=snapmatch
# （可选）云数据库常见：开启 SSL
# DB_SSL=true

# （可选）用于初始化管理员账号（避免把密码/哈希写进命令行历史）
# SEED_ADMIN_PASSWORD_HASH=
# 访客密码hash
# SEED_VISITOR_PASSWORD_HASH=

# ==================== 七牛云存储配置 ====================
# 存储提供商选择：qiniu（七牛云，当前）或 cos（腾讯云，未来）
STORAGE_PROVIDER=qiniu

# 七牛云访问密钥（获取方式：登录七牛云控制台 -> 密钥管理）
QINIU_ACCESS_KEY=
QINIU_SECRET_KEY=

# 七牛云存储空间名称
QINIU_BUCKET=

# 七牛云存储区域（华东 z0 / 华北 z1 / 华南 z2 / 北美 na0 / 亚太 as0）
# 官方文档：https://developer.qiniu.com/kodo/1672/region-endpoint-faq
QINIU_REGION=z0

# 七牛云 CDN 加速域名（绑定到存储空间的域名）
# 格式：https://cdn.yourdomain.com 或 http://xxx.clouddn.com（七牛云提供的默认域名）
QINIU_DOMAIN=

# 七牛云上传端点（可选，默认自动选择）
# QINIU_UPLOAD_URL=https://upload.qiniup.com
