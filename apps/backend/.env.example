# 本地开发示例（默认监听 3002，避免与其他服务端口冲突）
PORT=3002
NODE_ENV=development

# CORS allow origin for apps/admin
ADMIN_ORIGIN=http://localhost:3001

# JWT
JWT_SECRET=change-me
JWT_EXPIRES_IN=12h

# Auth sessions (refresh token)
AUTH_REFRESH_TOKEN_TTL_DAYS=30

# 数据库配置（通用于本地/自建/云 MySQL）
# 约定：统一使用 DB_*。
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=snapmatch_user
DB_PASSWORD=
DB_DATABASE=snapmatch
# （可选）云数据库常见：开启 SSL
# DB_SSL=true

# （可选）用于初始化管理员账号（避免把密码/哈希写进命令行历史）
# SEED_ADMIN_PASSWORD_HASH=
# 访客密码hash
# SEED_VISITOR_PASSWORD_HASH=

# ==================== 存储提供商 ====================
# 当前推荐：本地开发使用 Cloudflare R2；生产环境后续可切换腾讯云 COS（尚未实现）。
STORAGE_PROVIDER=r2

# ==================== Cloudflare R2（本地开发推荐） ====================
# 存储提供商：r2（Cloudflare R2，S3 兼容）
#
# 使用方式：
# - 设置 STORAGE_PROVIDER=r2
# - 通过 /assets/sign 创建分片上传（uploadStrategy=s3-multipart）
# - 前端按 part 请求 /assets/multipart/sign-part 获取预签名 URL 后直传
#
# 账号与密钥：Cloudflare Dashboard -> R2 -> API Tokens
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

# 端点（S3 API）：https://<accountid>.r2.cloudflarestorage.com
R2_ENDPOINT=
R2_BUCKET=

# （可选）公开访问域名（用于“作品集素材”返回公开 URL），可配置为自定义域名/Worker 域名
# 如果不配置，后端会在 R2 下为作品集素材回退返回临时签名 URL（便于联调）。
R2_PUBLIC_DOMAIN=

# （可选）分片大小（字节，需 >= 5MB；默认 8MB）
# R2_PART_SIZE_BYTES=8388608

# ==================== 腾讯云 COS（生产预留） ====================
# 注意：当前后端未实现 cos provider，仅保留占位说明。
# STORAGE_PROVIDER=cos
# COS_SECRET_ID=
# COS_SECRET_KEY=
# COS_BUCKET=
# COS_REGION=
# COS_PUBLIC_DOMAIN=
