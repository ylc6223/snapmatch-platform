# 生产环境示例（Docker/CloudRun）
#
# 建议容器内固定监听 3000（与 Dockerfile/健康检查一致），宿主机端口映射由部署脚本负责。
# 例如：`-p 3002:3000`，宿主机用 `http://localhost:3002/health` 做探活。

PORT=3000
NODE_ENV=production

# CORS allow origin for apps/admin
ADMIN_ORIGIN=https://your-admin-domain.example

# JWT
JWT_SECRET=change-me
JWT_EXPIRES_IN=12h

# Auth sessions (refresh token)
AUTH_REFRESH_TOKEN_TTL_DAYS=30

# CloudBase Node SDK（使用数据模型作为唯一存储层）
CLOUDBASE_ENV=
CLOUDBASE_REGION=ap-shanghai
CLOUDBASE_SECRET_ID=
CLOUDBASE_SECRET_KEY=

# CloudBase 数据模型名称（可选，不填则使用默认值）
# Runtime（鉴权）
CLOUDBASE_MODEL_USERS=rbac_users
CLOUDBASE_MODEL_AUTH_SESSIONS=auth_sessions

# Seed（RBAC 结构与预置数据）
CLOUDBASE_MODEL_RBAC_ROLES=rbac_roles
CLOUDBASE_MODEL_RBAC_PERMISSIONS=rbac_permissions
CLOUDBASE_MODEL_RBAC_USER_ROLES=rbac_user_roles
CLOUDBASE_MODEL_RBAC_ROLE_PERMISSIONS=rbac_role_permissions
CLOUDBASE_MODEL_RBAC_ROLE_DATA_SCOPES=rbac_role_data_scopes

# （可选）用于初始化管理员账号（避免把密码/哈希写进命令行历史）
# SEED_ADMIN_PASSWORD_HASH=
# 访客密码hash
# SEED_VISITOR_PASSWORD_HASH=
