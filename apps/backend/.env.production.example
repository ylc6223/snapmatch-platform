# 生产环境示例（Docker/CloudRun）
#
# 建议容器内固定监听 3000（与 Dockerfile/健康检查一致），宿主机端口映射由部署脚本负责。
# 例如：`-p 3002:3000`，宿主机用 `http://localhost:3002/health` 做探活。

PORT=3000
NODE_ENV=production

# CORS allow origin for apps/admin
ADMIN_ORIGIN=https://your-admin-domain.example

# JWT
JWT_SECRET=change-me
JWT_EXPIRES_IN=12h

# Auth sessions (refresh token)
AUTH_REFRESH_TOKEN_TTL_DAYS=30

# 数据库配置（通用于本地/自建/云 MySQL）
DB_HOST=
DB_PORT=3306
DB_USERNAME=
DB_PASSWORD=
DB_DATABASE=snapmatch
# DB_SSL=true

# （可选）用于初始化管理员账号（避免把密码/哈希写进命令行历史）
# SEED_ADMIN_PASSWORD_HASH=
# 访客密码hash
# SEED_VISITOR_PASSWORD_HASH=
