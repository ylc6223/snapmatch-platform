/**
 * @snapmatch/shared-types
 *
 * 前后端共享的类型定义包
 *
 * 使用方式：
 * - 前端：import { Project, ProjectStatus } from '@snapmatch/shared-types'
 * - 后端：import { Project, CreateProjectDto } from '@snapmatch/shared-types'
 */

// Project 相关类型
export * from './project';
